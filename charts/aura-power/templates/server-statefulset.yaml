{{- if .Values.server.enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "aura-power.server.fullname" . }}
  labels:
    {{- include "aura-power.server.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.server.replicas }}
  serviceName: {{ include "aura-power.server.fullname" . }}-headless
  selector:
    matchLabels:
      {{- include "aura-power.server.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "aura-power.server.selectorLabels" . | nindent 8 }}
        {{- with .Values.server.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        checksum/auth-values: {{ printf "%s|%s|%s" .Values.server.auth.existingSecret .Values.server.auth.jwtSecret .Values.server.auth.initialAdmin.password | sha256sum }}
      {{- with .Values.server.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "aura-power.server.serviceAccountName" . }}
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
        fsGroup: 65534
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: server
          image: {{ include "aura-power.image" (dict "root" . "image" .Values.server.image) | quote }}
          imagePullPolicy: {{ .Values.server.image.pullPolicy }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: {{ .Values.server.port }}
              protocol: TCP
          env:
            - name: API_PORT
              value: {{ .Values.server.port | quote }}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.server.auth.existingSecret | default (printf "%s-secret" (include "aura-power.server.fullname" .)) }}
                  key: jwt-secret
            - name: AUTH_DB_PATH
              value: "/data/aura-power.db"
            - name: ADMIN_USERNAME
              value: {{ .Values.server.auth.initialAdmin.username | quote }}
            - name: ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.server.auth.existingSecret | default (printf "%s-secret" (include "aura-power.server.fullname" .)) }}
                  key: admin-password
            - name: ACCESS_TOKEN_TTL
              value: {{ .Values.server.auth.accessTokenTTL | quote }}
            - name: REFRESH_TOKEN_TTL
              value: {{ .Values.server.auth.refreshTokenTTL | quote }}
            - name: CONTROL_NAMESPACE
              value: {{ .Release.Namespace | quote }}
            {{- if .Values.server.prometheus.url }}
            - name: PROMETHEUS_URL
              value: {{ .Values.server.prometheus.url | quote }}
            {{- end }}
            {{- if .Values.server.opencost.url }}
            - name: OPENCOST_URL
              value: {{ .Values.server.opencost.url | quote }}
            {{- end }}
            {{- with .Values.server.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 10
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.server.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
        {{- if not .Values.server.persistence.enabled }}
        - name: data
          emptyDir: {}
        {{- end }}
      {{- with .Values.server.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.server.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.server.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
  {{- if .Values.server.persistence.enabled }}
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes:
          {{- toYaml .Values.server.persistence.accessModes | nindent 10 }}
        {{- if .Values.server.persistence.storageClass }}
        storageClassName: {{ .Values.server.persistence.storageClass | quote }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.server.persistence.size }}
  {{- end }}
{{- end }}
