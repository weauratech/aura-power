apiVersion: apps/v1
kind: Deployment
metadata:
  name: contended-deployment
  namespace: __NAMESPACE__
  annotations:
    aura.sh/power-eligible: "true"
spec:
  replicas: 2
  selector:
    matchLabels: {app: contended-deployment}
  template:
    metadata:
      labels: {app: contended-deployment}
    spec:
      containers:
        - name: pause
          image: registry.k8s.io/pause:3.10
---
apiVersion: v1
kind: Service
metadata:
  name: contended-statefulset
  namespace: __NAMESPACE__
spec:
  clusterIP: None
  selector: {app: contended-statefulset}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: contended-statefulset
  namespace: __NAMESPACE__
  annotations:
    aura.sh/power-eligible: "true"
spec:
  serviceName: contended-statefulset
  replicas: 1
  selector:
    matchLabels: {app: contended-statefulset}
  template:
    metadata:
      labels: {app: contended-statefulset}
    spec:
      containers:
        - name: pause
          image: registry.k8s.io/pause:3.10
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: contended-cronjob
  namespace: __NAMESPACE__
  annotations:
    aura.sh/power-eligible: "true"
spec:
  schedule: "0 0 31 2 *"
  suspend: false
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
            - name: pause
              image: registry.k8s.io/pause:3.10
